import './App.css';

function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <span className="eyebrow">Voice AI assistant</span>
        <h1>Let visitors talk to your agent directly.</h1>
        <p>
          This React app embeds a Vapi widget so users can start a voice conversation
          from the page without leaving the site. Click the floating button in the bottom-right
          corner to start a voice call.
        </p>
      </section>
    </main>
  );
}

export default App;